import { describe, expect, it } from "vitest";
import { CreateReviewUseCase } from "./create.review.usecase.js";
describe("CreateReviewUseCase", () => { it("사람 검토 후보를 만든다", () => { expect(CreateReviewUseCase).toBeDefined(); }); });
