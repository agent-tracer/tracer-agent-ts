import { describe, expect, it } from "vitest";
import { CreateExperimentUseCase } from "./create.experiment.usecase.js";
describe("CreateExperimentUseCase", () => { it("실험 생성 기능을 제공한다", () => { expect(CreateExperimentUseCase).toBeDefined(); }); });
