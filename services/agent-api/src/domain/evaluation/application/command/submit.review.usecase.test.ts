import { describe, expect, it } from "vitest";
import { SubmitReviewUseCase } from "./submit.review.usecase.js";
describe("SubmitReviewUseCase", () => { it("사람 검토를 제출한다", () => { expect(SubmitReviewUseCase).toBeDefined(); }); });
