import { describe, expect, it } from "vitest";
import { StartExperimentUseCase } from "./start.experiment.usecase.js";
describe("StartExperimentUseCase", () => { it("실험 시작 기능을 제공한다", () => { expect(StartExperimentUseCase).toBeDefined(); }); });
