import { describe, expect, it } from "vitest";
import { GetExperimentUseCase } from "./get.experiment.usecase.js";
describe("GetExperimentUseCase", () => { it("실험과 변형을 조회한다", () => { expect(GetExperimentUseCase).toBeDefined(); }); });
